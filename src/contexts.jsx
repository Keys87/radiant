import { createContext } from "react";
// import { Auth } from "firebase/auth"

export const AuthInstanceContext = createContext()
export const UserObjectContext = createContext()
export const TestContext = createContext("Log Out")
export const DBInstanceContext = createContext()